import styles from "./PollinationCarousel.module.scss"

import SectionWrapper from "../../layout/SectionWrapper"

function PollinationCarousel(color) {
  return <SectionWrapper color={color}></SectionWrapper>
}

export default PollinationCarousel
